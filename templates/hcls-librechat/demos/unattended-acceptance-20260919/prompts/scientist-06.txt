I am studying DNA continuation using the public Arabidopsis chloroplast NC_000932.1 reference. The frozen FASTA, example requests and provenance are under /workspace/scientist-06/study-inputs-v22. First determine whether the current Evo2 App supports likelihood or variant-effect scoring; do not present generation as an unavailable score.

For a bounded continuation study, use six zero-based reference start positions: 1000, 15000, 35000, 60000, 90000 and 120000. At each, use a 256-base reference prefix and request 64 new bases with seed 1 and seed 7, keeping other settings from the staged examples. That authorizes at most twelve new requests. Verify each prefix against the exact reference, and compare actual suffix length/alphabet, GC content, seed-to-seed diversity and agreement with the next 64 reference bases. Separate returned prefixes from newly generated sequence and preserve any genuinely returned probabilities with their actual meaning.

Save original inputs, outputs, coordinates, hashes, operation identities, measured timings with units, per-case results and a downloadable methods/limitations report under /workspace/scientist-06/unattended-20260919/chloroplast. Finish the whole study even if I close the browser, with persistent progress and downloads when I return, without a continuation message. Use the existing request allowance; do not change deployments or limits. This is not variant-effect validation or reproduction of the paper's full benchmarks.
