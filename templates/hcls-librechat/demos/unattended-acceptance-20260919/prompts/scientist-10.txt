I am studying visual augmentation of recorded robot demonstrations. Use the real ALOHA coffee inputs described in /workspace/scientist-10/study-inputs/recorded-aloha-v2.json and supplementary/qualification-source-provenance.json, not the older generated robot fixture. Perform one Cosmos MP4-to-MP4 augmentation of recorded-episode-0-cam_high.mp4 with cooler indoor lighting while preserving the robot, objects, camera geometry and motion. Then perform one supported LeRobot-to-LeRobot augmentation of aloha-coffee-two-recorded-segments.tar.zst for the high camera only. Keep the wrist camera and all recorded actions, states, booleans and timestamps unchanged. These two augmentations are the entire model-request budget; no retries with altered scientific settings to obtain a nicer result.

Check video readability, frame counts, timing, geometry and temporal artifacts. Compare episode identities, all nonvideo fields and unselected media with the originals. Export the actual recorded nonvideo data as NPZ, HDF5, zipped CSV and a standalone SQLite file so I can analyze it elsewhere; verify that each export preserves the complete original values. Explain any visual changes or unsupported behavior honestly. Numerical preservation alone does not prove valid visual-action alignment or useful policy-training data.

Save raw outputs, exact provenance, operation IDs, measured comparisons, all four exports and a downloadable methods/limitations report under /workspace/scientist-10/unattended-20260919/robotics. Carry the whole study through analysis and file delivery even if I close the browser. I should be able to return to progress and completed downloads without telling you to continue. Preserve all prior studies and keep my existing concurrency policy.
