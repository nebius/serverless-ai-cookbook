# Model output and customer-file publication repair

Source-only successor evidence; v56 remains unchanged. No scientific rerun,
operator correction of an admitted plan, or new provider call was used here.

## Original customer outcome

Scientist02's v56 whole study `fa0526b1-0399-5438-b7a8-017f6dbec2fa`
ran all four requested original predictions and all analyses after browser
disconnect, but failed final publication: `boltz2-1acb/input.json` does not exist.
The plan made the same invalid promise for 2PTC. Native client results never
included this file; the actual input remains its original hash-frozen workspace
file. The failed plan and receipt were not repaired.

Independent coordinate calculations verify the retained numerical reports:

| Original case | Global C-alpha RMSD, Å | Recovered reference contacts |
| --- | ---: | ---: |
| Boltz2 / 1ACB | 1.288704 | 52 / 63 |
| Boltz2 / 2PTC | 0.576614 | 65 / 68 |
| Protenix v2 / 1ACB | 17.018829 | 0 / 63 |
| Protenix v2 / 2PTC | 17.270950 | 0 / 68 |

These are descriptive agreement measurements, not affinity or biological
validation. Poor Protenix geometry remains a finding. The original requests
retain query-only/no-MSA inputs, one sample per case and explicit Protenix seed7;
Boltz has no exposed seed. Both Protenix opaque artifact files are actually
manifest-declared uncompressed mmCIF, hash-equal to the analyzed coordinates.

The full requested output archive contains310 files/2,802,935 bytes. Reopened
real Runs/Workspace delivered21 selected partial report, coordinate, metric,
request and provenance files (1,148,058 bytes), all hash-equal to the archive.
This proves partial file delivery, **not whole-study acceptance**.

## Bounded repair

- Native and batch discovery/preflight now use actual client filenames;
  batch artifact count remains unknown and `output-NN.artifact` remains
  manifest-dependent. Impossible aliases fail before admission and remain in
  the draft. Correct provenance binds the original input or preparation output;
  no bytes, filenames or scientific parameters are invented.
- Explicit final promises still require their actual verified files. A producer
  may additionally register exact customer outputs. Final publication verifies
  their completed-step membership/hash/size, deduplicates paths, and exposes
  step-qualified names. It never enumerates internal worker/checkpoint files.
  The clinical producer's explicit allowlist conditionally includes real drafts,
  source and review material; no-report outcomes cannot fabricate a draft.
- Native `tool_name` is optional and carried unchanged from typed discovery
  through immutable whole-study/workflow identity to the existing client's
  selector. Selection/validation remains in that client; no model-specific
  input changes or first-contract fallback are introduced by the study layer.
  Proven pre-admission native rejection survives an async ExceptionGroup as a
  bounded diagnostic (type/code/schema pointers/tool names), without raw input
  or exception text. Unknown admission and arbitrary errors remain unchanged.
- Boltz's actual `confidence_scores` and `ptm_scores` arrays are retained in
  full, with original order, numeric types and result-file hash provenance.
  No averaging, calibration or seed/rank inference replaces the arrays.
  Request-binding guidance points structure analysis to the original input or
  actual batch request, not a report heading or filename.

The original reports omitted `request_file` in every analysis and missed the
plural confidence arrays. Those omissions are retained. A separately labeled
offline successor reassessment preserves geometry/contact measurements and
binds the full original arrays and request bytes; it does not rewrite customer
reports or change the original failed outcome. Batch confidence artifacts
remain distinct evidence, not inferred from predicted coordinates.

## Verification and retained evidence

131 control-plane-environment study/discovery/clinical/cancellation/text tests,
74 scientific-environment structural/confidence/output tests and63 selector/
workflow/composer tests passed. Ruff (Python3.11 target), JavaScript syntax and
diff checks passed. Initial mixed-environment execution retained22 missing
`httpx2` failures; supported environments were then used explicitly, not
counted as product failures or hidden retries. Exact installed-image and fresh
natural-client qualification remain release-owner work.

Protected root `scientific-unattended-20260919/natural-v56/scientist-02`:

| Receipt | SHA-256 |
| --- | --- |
| terminal-capture-r1/study.json | `87ce5fa1876904e83bda9afbd6d69b849b6b95790c7f34dc4761ea4a9bf1db64` |
| terminal-files-r1/receipt.json | `33485f5f60191b670f7af236fe4f1033694ea9527d254e45deceff81e6703732` |
| terminal-browser-r1/receipt.json | `d71f6efd0132ca7147724b352491d61854bb9c6cf451998bf85771e3c3adcaa1` |
| independent-numerics-r1.json | `32da43a8f62eef0eab07a5aea598276ffb4e6686a2eab9b13f219b265057ce0a` |
| independent-provenance-r1.json | `da32bb8799450366a0dfc89292238c762d41618eb188d9a3145d7d78ec761c1e` |
| successor-confidence-reassessment-r1/receipt.json | `b8366cf9b79a3ecd06e852c2acc3b2a583c417ccbe035a6e053fd28ee6d31856` |

Full payloads, clinical source material, account manifests, credentials and
provider configuration are not copied into this portable note.
