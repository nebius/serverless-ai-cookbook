# Recorded robotics comparisons: delivery is not physical validity

The unchanged private v60/r12 robotics Study completed all five phases. Its two
top-level augmentation requests succeeded; the LeRobot request also records two
native child generations. Uploads are separate from inference. All 128 rows,
nine nonvideo fields and 6,144 scalar values preserve their original types/bits.
The unselected wrist camera is byte/pixel identical. NPZ, HDF5, zipped CSV and
SQLite independently reopen with all original values. Ten actual Runs downloads
(3,886,029 bytes) match registered hashes and the independent S3 snapshot. The
browser disconnected during the ongoing Study; final state-save/close is retained.

This is **not** a lighting-only or physical-action success. Independent inspection
of exact same-index source/output frames found strong blue/white highlights,
changed cup appearance/opacity and mottled texture; selected dataset frames also
change the gripper marker appearance. Coarse scene layout remains recognizable,
but sparse frames cannot establish whole-trajectory/contact fidelity. The saved
report states appropriate generic limits yet does not describe these actual
visual findings. Original outputs/report remain unchanged and visually partial.

The phrase “exactly two model calls” came from the planner-authored methods
section, not the deterministic robotics helper. Guidance now distinguishes
top-level requests from recorded child generations; unknown child counts stay
unknown. The CPU helper's zero-call field is explicitly scoped to that analysis.

The successor helper adds an exact visual-comparison index and available PNGs:
at most four matching zero-based decoded frames per native/selected dataset
shard pair, original row above generated row, without resizing or reordering.
Source/output hashes, image hashes and frame indices are retained. Metadata
mismatches remain explicitly unavailable, never implicitly aligned. Only those
hash-bound files are automatically registered as customer downloads. Captions
state that visual comparison is not physical/action-validity proof. There is no
new inference, dependency, API call, vision model or budget/timeout change.

## Verification

- All eight original robotics cases remain; seven added cases cover exact RGB
  pixels/order at 1, 2, 7 and 128 frames, unavailable alignment, registered-image
  hash mismatch, and discovery/guidance. Fifteen robotics cases pass.
- Output/publication/discovery integration passes in the existing appropriate
  interpreters. The initial combined local command used an evaluation interpreter
  without `httpx2`: four unrelated import failures are retained, then those
  contract cases pass in the control-plane interpreter without source changes.
- Offline replay of actual v60 bytes produces two comparison PNGs while preserving
  every previous media/numerical measurement. Only a private native-envelope path
  was rebound to an exact byte-identical video copy; original artifacts remain
  immutable. No successor installed/live acceptance is claimed by this replay.

Protected payload-free receipt hashes:

- Original final outcome: `c0080fc579b2c3dd16f974a3598990f93a659a347706a7818b44949ee30e8ae9`.
- Independent values/media/exports: `b46cd2d88652e7cc6fbea6e6d3206fc20ec1bd8e195a948de08c4f9a67caee6c`.
- Successor offline actual-byte replay: `3f54ff11bbec7b4c1d84ac55722376e5b3fc487080e8ca1a1a5596de657b0bae`.

Raw media, private messages, credentials and personal manifests are not copied
into this portable note. Customer Rene and cloud resources are unchanged.
