# Optional presentation heading after actual v60 composer rejection

The private-EU scientist04 v60/r12 natural turn ended without admission. Three composer calls were rejected by the installed client schema because their `report` step omitted `arguments.title`. The persisted assistant response was terminal (`unfinished=false`, `error=false`), asked for another turn, and never called the whole-study launcher. The sole bounded observer recorded no matching Study and did not close the browser. A later authorized terminal snapshot/API capture confirmed only older terminal studies; the browser state was then saved and closed. This is a failed natural delivery, not disconnect/resume proof.

The source correction makes only the report document heading optional. Omission renders the neutral **Scientific study report** heading; explicit titles remain verbatim and must still be strings, nonblank and one-line. Section titles, files, formats, cardinality, scientific parameters, input verification, plan identity and admission are unchanged. Admission metadata preflight and the generated report-helper manifest use the same documented default. The immutable plan itself is not rewritten. The report helper's existing complete-manifest contract remains strict.

## Focused evidence

- 98 Python tests passed: new presentation-default tests and existing report metadata, report assembly and immutable composer cases. XML SHA256: `a81688e7707f6c6e6fc3abb5f55d773361d58ab3d32b664c796d969c6e536f6b`.
- Three Node boundary tests passed using v60's actual installed `DynamicStructuredTool` and MCP SDK, a fresh network-none container, candidate Python source mounted read-only, and temporary local workspace. All three exact previously rejected argument objects reached the real composer. The first grouped edit became usable; its exact repeat reused the revision. The later diagnostic draft correctly remained unfinalized because it referenced a nonexistent dummy input; no missing inputs/dependencies were repaired or admitted.
- The boundary gate also rejected explicit wrong-type/blank/newline headings before transport, then composed, admitted, ran and published a CPU-only report without a document heading. Original numeric source bytes containing `4.55` and `4.325` were retained exactly. Python cases separately completed explicit and long-title reports without changing their headings.
- Ruff and `git diff --check` passed. Early test-only failures (an old schema object-identity assertion, an overly narrow expected-error regex, and a mistyped suite filename) were corrected; they were not live application failures.

The candidate-source mount means this is **source plus pinned-client-boundary evidence**, not qualification of a new built image or a repaired natural run. No provider/model calls, customer changes, budget increases, alternate prompts or follow-ups were performed. Root will aggregate other observed outcomes before building a successor.

## Protected receipts

Under the campaign's protected root:

- `natural-v60-private04/scientist-04/bounded-diagnosis-r1/messages.json`: SHA256 `a3d4c05a33624799e5c4398111b191c8962b88b3c74b553469cbbca0e653420d`.
- The same folder's `receipt.json`: `80719838982655a33df351e6a1fad63c27428acd50f368b0b8abd3f61cb0b480`.
- The same folder's `terminal-close-receipt.json`: `68fefa3a2457bb681c32a7501cfa7b0fe89e826a174078f0bb63dcf705eee8b3`.
- `report-title-source-tests-r3.xml` records the 98-test result above.

`test-report-title-boundary.cjs` uses `SCIENTIFIC_REPORT_CANDIDATE` for the candidate runtime directory and `SCIENTIFIC_REPORT_RETAINED_MESSAGES` for the read-only retained trace. A later installed-image gate must omit the candidate override, import actual `/opt/bionemo` modules, mount the trace as test evidence, and require zero skips. The reference v60 OCI index was `sha256:6d764aca3237cec1ccd234690a191ad89247e08d3f60b7b1f4f1722a6a414dca`.
