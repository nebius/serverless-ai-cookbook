# Integrated workbench v61: installed acceptance and partial live rollout

Recorded 20 September 2026. **Not customer-ready:** nine of ten replacement
workbenches could not be provisioned, so the required integrated cohorts and
customer upgrade remain incomplete. Each user still has a separate LibreChat
instance, even when several users share one tenant bucket. Model Apps remain
shared platform services.

## Exact release

| Identity | Value |
| --- | --- |
| Application source | `7524b8c33e4e7d7cc92af51c0e61b8ff46ab5b33` |
| Published tag | `cr.eu-north1.nebius.cloud/e00akg9ndpx77eaexh/lc:r0919-v61-7524b8c-p2` |
| OCI index | `sha256:afe3a1d1de250a6a5aed0878839ab27725e600cb6b934259a33cf5ca6d5aa215` |
| Linux/amd64 runtime | `sha256:6c3884cb81e36c06cf63124c2806da046c90adbb6360585def5b3391e79208dd` |
| Backend | Helm 190, unchanged control-plane digest `sha256:e4319840c1d9378c2942f2e389785917e045af4b5733f93da3b535c3f33e3dad` |
| Private-instance identity method | Immutable provider image, exact local installed-image gate, authenticated owner/configuration/storage APIs; not remote filesystem/environment attestation |

The first export was a standalone OCI manifest and failed the publication
verifier before installed acceptance or deployment. The retained `-p2` export
uses explicit build provenance and a verified index. Application bytes and
source did not change between exports.

## Repairs included

| Observed failure in the preceding cohort | Implemented repair |
| --- | --- |
| A report omitted its presentation-only title and could not be submitted | Neutral default only when the title is omitted; explicit titles and scientific validation remain strict |
| Invalid composed plans returned generic tool errors | Bounded actionable schema paths through the actual installed tool factory; invalid work is not dispatched |
| A nonterminal observation slice looked like terminal failure | Waiting/reconnection labels preserve the original state and operation |
| A valid PDB was rejected because a planner promised a CIF filename | Byte-preserving `prediction.structure` alias and preflight rejection of guessed future extensions |
| A structure manifest was parsed as a coordinate file | Explicit typed manifest/index selection, retaining original coordinates and same-operation confidence |
| Gzip batch input was declared uncompressed when the field was omitted | Derive omission only from the published contract and matching byte signature; explicit choices are not overwritten |
| Patient reports appeared as unqualified clinical findings | Both existing extraction/review passes supply attribution; missing/conflicting attribution remains unclear |
| Robotics reports deferred visual comparison and confused request populations | Registered hash-bound same-index frame comparisons, with sparse-review limits and guidance distinguishing parent requests from child generations |

Original failed outputs were not edited or rescored as successes. In the prior
v60 cohort, 01/02/03/04 failed, 08 remained clinically partial, and 10 remained
visually/report-wise partial despite correct numeric exports. 05/06/09 passed
their scoped checks; 07 had no provisioned instance.

## Installed-image evidence

The published image passed 419 domain cases (including all 331 preceding
cases), 37 installed Graph/ToolNode/Run tests, 10 publication-lock cases,
11 SDK cases, and seven CPU studies with 41 independently verified files.
Separate installed groups passed 14 validation-factory and three report-title
SDK cases. Thirty-six runtime hashes were bound. There were no skipped cases,
runtime-source overlays, external network access, or hosted-model calls.

Thirteen source UI tests and compiled-marker presence are recorded separately;
they are **not** live UI acceptance.

Installed acceptance SHA256:
`46b728e8845695cabb887459a08f449fb00550a5fcb85b7395b22b2785dea085`.

## Actual customer-path evidence: scientist 07 only

Endpoint `aiendpoint-e00d2bx2yzwyjg7q4p` received one unchanged, uncoached r13
prompt. Its browser closed while Study `048bdbed-adb7-5958-8cb3-a6fa70a4bf4a`
was running. All seven phases and four native operations completed.

- Independent checks recalculated 32 PhenoAge and 17 AltumAge predictions.
- The report correctly disclosed **one** overlapping sample rather than the
  requested maximum of 16; four negative predictions were retained.
- Maximum numerical differences were `3.0198e-14` and `1.5530e-5` years.
- The persisted acknowledgement contained the exact queued Study ID and Runs
  link without premature scientific/count claims.
- All 18 actual Runs/Workspace downloads, totaling 10,764,764 bytes, matched
  registered and independently collected hashes.
- The final browser state was saved and closed. No follow-up prompt, password
  fallback, stale-cookie merge, manual output repair, or new budget was used.

Scoped live receipt SHA256:
`67a75bdc368eeb9add226feb5bfb4fe7b72c3643289df45f2224333e5a9f10ae`.
This does not establish clinical validity or qualify the remaining workflows.

## Provider blocker and retained state

The other nine private-IP creates returned provider `Internal`. Exact
project/name reads returned `NotFound` for every failed create. No public IP
or SSH was requested. The successful 07 backing VM was independently observed
to have only a private network interface. Current tenant CPU, VM and disk
usage were below their listed limits; these facts do not establish the cause.
The existing operator Slack blocker thread contains the request/trace IDs.
There is no blind create loop or quota change.

Before replacement, all nine prior test owners had current inactive/terminal
API checks and retained chat/configuration/caller-history evidence, plus 1,616
downloaded r12 files (22,060,367 bytes). STOPPED was then confirmed by provider
GET for each exact endpoint. Buckets and keys were retained. These private
no-SSH archives are not full Mongo/local-disk backups. A retirement-helper
path-type error was fixed and reproduced in offline regression tests before
the first cloud mutation; it did not affect application runtime code.

Rene's actual instance is unchanged. The exact v61 local restore rehearsal
preserved 49 collections, 81 documents, 334 indexes, two state files, eight
seeded agent identities and original login/history before and after restart.
Its local container was stopped. Empty custom-agent/upload categories are not
positive migration coverage. Receipt SHA256:
`d8a21dcc0a620c7af310def4fa8e54b03df75d20f80f3d21ee63af377659d2d5`.

[Nebius documents that Endpoint STOP removes container-disk data](https://docs.nebius.com/serverless/overview).
Persistent bucket artifacts survive separately. Planned backup/restore and a
local Docker restart do not prove recovery of chats/settings/encryption state
after unexpected cloud-instance loss. The operator has been asked whether
durable private state per user should be added; no such architecture change or
resource was silently introduced.

## Continuation

Resolve provider creation failures, reconcile existing names before any
authorized retry, finish exact per-owner bindings, and run the remaining
frozen prompts. Prepared verifiers are held; historical results do not qualify
them. Preserve failures and complete two unchanged-release clean cohorts before
claiming customer readiness or performing a fresh state-preserving customer
cutover. No concurrency, context, output, retry, or readiness limits were raised.

Protected receipts and payloads remain outside Git under
`/home/tux/secure-handoff/scientific-unattended-20260919`. This document contains
only identities, hashes, counts, limitations and the continuation plan.
