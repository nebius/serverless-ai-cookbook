# Verified completed-study summary (source-only successor)

The v57 natural MindEval study completed its deterministic analysis and delivered
five real Runs downloads (45,822 bytes), matching published hashes and independent
Object Storage reads. It retained six original full records, 126 literal messages,
30 supplied criterion scores, five distinct criteria and ten profile×criterion
cells. No new consultations or judgments were submitted.

The final planner chat nevertheless put **4.325** in the GLM/profile-032 cell;
that exact run's stored `state.judgment.overall_score` is **4.55**. The row-level
saved report was correct. The original chat remains unchanged and this cohort is
not a clean end-to-end numerical pass. The browser close-response timestamp is
0.203 seconds after the saved completion timestamp; strict precompletion
disconnect was not established for this very short study.

Protected original outcome: `U/independent-v57/scientist-09-final-receipt.json`,
SHA256 `ec812187855cc3fc41bc26a66b84b60cf0dcde42093da0f0102b8863ae584e5b`.
Here `U` denotes the private scientific-unattended-20260919 evidence root; raw
records, conversations and credentials are not published in this repository.

## Bounded repair

The existing MindEval helper adds `customer-summary.json` to its closed,
hash-verified bundle. Every overall score is copied directly from its original
record, on a row containing profile ID, clinician model and run ID. Missing or
non-numeric overall scores remain unavailable even if criterion scores exist.
There is no new mean, ranking, winner, scoring algorithm or model call.

The existing registered-customer-artifact path adds this one file to final
publication; the five declared report/CSV/measurement/provenance files remain.
Completed-study status exposes the saved summary text only after checking the
registered file, publication metadata and actual bytes. Runs renders that same
text and its authenticated workspace link. A 64-KiB **preview** ceiling returns
a full-file link and explicit notice, never a truncated table or a rejected
study. Older studies have no new summary panel. Missing/corrupted preview bytes
do not change saved study state or produce invented measurements.

Short discovery and agent guidance instructs clients to quote the verified text
and identities instead of recomputing or transposing a new chat table. This is
not proof that an untested future planner will comply.

## Tests and retained-source replay

- 116 focused Python tests passed across the new summary regression and existing
  MindEval, report, customer-publication and semantic-preflight suites.
- Four actual React server-render tests passed using the pinned v57 image's
  React/TypeScript packages and candidate source as **test input**. This is not
  an installed successor-image or live browser acceptance claim.
- A separate offline replay of the exact six retained records preserved the
  previous `report.md`, `runs.csv`, `scores.csv` and `measurements.json` bytes.
  All six keyed overall values matched their source records; 22 helper manifest
  files verified. New provenance honestly binds the candidate helper and replay
  plan paths, leaving original provenance unchanged.

Replay: `U/mindeval-customer-summary-replay-20260919T203838826566Z/receipt.json`,
SHA256 `fdbdf26bdf3ef3f89380fcccdae1ba7e9129dd44fe036b5c04af37d731d8ad2b`.

New tests: `test_mindeval_customer_summary.py` (18 cases) and
`demos/completion-summary.test.cjs` (four cases). The root integrator owns the
shared Study/schema freeze, combined build and later live acceptance. No cloud
change, inference, quota/limit increase or repair of the original Study occurred.
